import { Options } from './types';

export const defaults: Options = {
	initialisedClass: '',
	activePanelClass: '',
	activeTriggerClass: '',
	triggerClass: true,
	animate: true,
	multiselect: true,
};
