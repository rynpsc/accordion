import { Options } from './types';

export const defaults: Options = {
	initialisedClass: '',
	activePanelClass: '',
	activeHeaderClass: '',
	headerButtonClass: '',
	animate: true,
	multiselect: true,
	dataAttributePrefix: 'accordion',
};
